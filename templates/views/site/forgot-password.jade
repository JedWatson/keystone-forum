extends ../../layouts/default

block css
	link(href="/styles/session.min.css", rel="stylesheet")

block content
	.container-sm
		form(method='post', novalidate).session-form
			input(type='hidden', name='action', value='send-password')
			h2 Reset your password
			+flash-messages(messages)
			.form-group
				label.sr-only Email
				input(type='email', name='email', placeholder='Email').form-control
			button(type='submit').btn.btn-primary.btn-block.btn-submit Reset
		p.text-center
			a(href='/login') Log in

block js
	script.
		var $form = $('.forgot-password');
		
		var $submit = $form.find('.btn-submit');
		
		$submit.click( function() {
			$submit.addClass( 'disabled' );
		});
		
		$form.find("input[type!='hidden']").first().focus();
